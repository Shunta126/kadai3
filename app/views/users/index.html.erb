<%= render 'user', user: @user %>

<%= render 'book', book: @book %>

<table>
  <h1>Users</h1>
  <tr>
    <th>image</th>
    <th>name</th>
  </tr>
<% @users.each do |user| %>
  <tr>

<td>写真</td>
<td><%= user.name %></td>
<td><%= link_to "show", user_path(user.id)%></td>
</tr>
<% end %>
</table>