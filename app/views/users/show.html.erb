<%= render 'user', user: @user %>

<%= render 'book', book: @book %>

<h1>Books</h1>
<table>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  <% @books.each do |book| %>
<tr>
<td>写真</td>
<td><%= book.title %></td>
<td><%= book.body %></td>
</tr>
<% end %>
</table>

 <footer>
    <a>CanpyRight Infratop.inc</a>
  </footer>
</table>